# Serve /api/v1 from local fixtures (true) or call the real gateway (false).
VITE_API_MOCKS=true
# Where the Vite dev server proxies /api (gateway; for now Catalog Service from docker compose).
ICYRE_API_PROXY=http://localhost:8081
