name: icyre

# Local development stand: `docker compose up -d` starts everything.
# Host ports: Catalog API 8081 · PostgreSQL 5432 · Kafka 9094 · Jaeger UI 16686
#             Prometheus 9090 · Grafana 3000 (admin / admin)

x-catalog-env: &catalog-env
  APP_ENV: local
  LOG_LEVEL: info
  DATABASE_URL: postgres://icyre:icyre@postgres:5432/icyre?sslmode=disable
  KAFKA_BROKERS: kafka:9092
  OTEL_ENABLED: "true"
  OTEL_EXPORTER_OTLP_ENDPOINT: jaeger:4318

services:
  # --- core -----------------------------------------------------------------
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: icyre
      POSTGRES_USER: icyre
      POSTGRES_PASSWORD: icyre
    ports: ["5432:5432"]
    volumes: ["postgres-data:/var/lib/postgresql/data"]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U icyre -d icyre"]
      interval: 5s
      timeout: 3s
      retries: 20

  kafka:
    image: apache/kafka:3.9.1
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_LISTENERS: INTERNAL://:9092,EXTERNAL://:9094,CONTROLLER://:9093
      KAFKA_ADVERTISED_LISTENERS: INTERNAL://kafka:9092,EXTERNAL://localhost:9094
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: INTERNAL:PLAINTEXT,EXTERNAL:PLAINTEXT,CONTROLLER:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: INTERNAL
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka:9093
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
    ports: ["9094:9094"]
    volumes: ["kafka-data:/var/lib/kafka/data"]
    healthcheck:
      test: ["CMD-SHELL", "/opt/kafka/bin/kafka-broker-api-versions.sh --bootstrap-server localhost:9092 > /dev/null 2>&1"]
      interval: 10s
      timeout: 10s
      retries: 20
      start_period: 15s

  kafka-init:
    image: apache/kafka:3.9.1
    entrypoint: ["/bin/bash", "/scripts/create-topics.sh"]
    volumes: ["./deploy/kafka/create-topics.sh:/scripts/create-topics.sh:ro"]
    depends_on:
      kafka: { condition: service_healthy }
    restart: "no"

  # --- services -------------------------------------------------------------
  catalog-migrate:
    build: { context: ., dockerfile: services/catalog/Dockerfile }
    image: icyre/catalog:local
    command: ["migrate"]
    environment:
      <<: *catalog-env
      KAFKA_ENABLED: "false"
      OTEL_ENABLED: "false"
    depends_on:
      postgres: { condition: service_healthy }
    restart: "no"

  catalog:
    image: icyre/catalog:local
    environment: *catalog-env
    ports: ["8081:8080"]
    depends_on:
      catalog-migrate: { condition: service_completed_successfully }
      kafka-init: { condition: service_completed_successfully }
    restart: unless-stopped

  # --- observability --------------------------------------------------------
  jaeger:
    image: jaegertracing/jaeger:2.10.0
    ports:
      - "16686:16686" # UI
      - "4318:4318"   # OTLP/HTTP
    # The image is distroless (no shell for a healthcheck); Jaeger is not a
    # dependency of any service, exporters retry on their own.

  prometheus:
    image: prom/prometheus:v3.6.0
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.path=/prometheus
    ports: ["9090:9090"]
    volumes:
      - ./deploy/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:9090/-/healthy"]
      interval: 10s
      timeout: 3s
      retries: 10

  grafana:
    image: grafana/grafana:12.2.0
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: admin
      GF_USERS_ALLOW_SIGN_UP: "false"
    ports: ["3000:3000"]
    volumes:
      - ./deploy/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./deploy/grafana/dashboards:/var/lib/grafana/dashboards:ro
      - grafana-data:/var/lib/grafana
    depends_on:
      prometheus: { condition: service_healthy }
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3000/api/health || exit 1"]
      interval: 10s
      timeout: 3s
      retries: 10

volumes:
  postgres-data:
  kafka-data:
  prometheus-data:
  grafana-data:
